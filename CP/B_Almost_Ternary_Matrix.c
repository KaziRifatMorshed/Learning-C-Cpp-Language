#include <stdio.h>
#define black 1
#define white 0

int main(void) {
  int t = 0;
  unsigned int n = 0, m = 0; // n = row, m = column

  scanf("%d", &t);
  while (t--) {
    scanf("%d %d", &n, &m);
    getchar();
  }
}