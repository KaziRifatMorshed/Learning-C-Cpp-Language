#include "stdio.h"
// incomplete
//  printf("");
//  scanf("%",&);
int main() {
  int i, number = 0;
  printf("input your number = \n");
  scanf("%d", &number);
  printf("Prime factors are : ")

      for (i = 1; i <= (number / 2); i++) {
    if (expression) {
    }
  }

  return 0;
}